/**
 * 중앙화된 data-testid 상수
 *
 * 실제 컴포넌트에 data-testid를 추가할 때 이 값들을 사용합니다.
 *
 * 주의: 이 파일은 src/config/test-ids.ts를 re-export합니다.
 * 실제 정의는 src/config/test-ids.ts에 있으며, Next.js가 접근 가능합니다.
 */

export { TEST_IDS } from '../../src/config/test-ids';
